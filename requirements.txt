# Root requirements for Vercel Python runtime (api/index.py).
# Keep it minimal: the engine is stdlib-only, the app needs FastAPI.
# httpx powers the shared campus member store (Upstash/Vercel-KV REST).
fastapi>=0.110
httpx>=0.27
