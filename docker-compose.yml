services:
  api:
    build: ./backend
    ports: ["8000:8000"]
    volumes: ["./data:/app/data:ro", "./experiments:/app/experiments"]
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    depends_on: [api]
